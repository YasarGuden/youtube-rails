<%= form_with(model: film, local: true) do |form| %>
  <% if film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(film.errors.count, "error") %> prohibited this film from being saved:</h2>

      <ul>
      <% film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :isim %>
    <%= form.text_field :isim %>
  </div>

  <div class="field">
    <%= form.label :yapımcı %>
    <%= form.text_field :yapımcı %>
  </div>

  <div class="field">
    <%= form.label :puan %>
    <%= form.number_field :puan %>
  </div>

  <div class="field">
    <%= form.label :yorum %>
    <%= form.text_area :yorum %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
